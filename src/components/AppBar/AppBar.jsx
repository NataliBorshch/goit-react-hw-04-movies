import React from "react";
import Navigation from "../Navigation";
import "./AppBar.scss";
const AppBar = () => {
  return (
    <header>
      <Navigation />
    </header>
  );
};

export default AppBar;
